<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, as: :select, collection: @cuisine %>
  <%= f.button :submit, class: 'btn btn-primary rounded' %>
<% end %>
